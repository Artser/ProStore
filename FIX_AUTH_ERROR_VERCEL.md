# Исправление ошибки "There was a problem with the server configuration" на Vercel

## 🔍 Проблема

После настройки Google OAuth появляется ошибка:
```json
{"message":"There was a problem with the server configuration. Check the server logs for more information."}
```

## ✅ Решение

Эта ошибка возникает, когда не настроены переменные окружения на Vercel. Проверьте следующее:

### Шаг 1: Проверьте переменные окружения на Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект **pro-store-nine**
3. Перейдите в **Settings** → **Environment Variables**
4. Убедитесь, что есть **ВСЕ** следующие переменные:

#### Обязательные переменные:

1. **AUTH_SECRET** (обязательно!)
   - Значение: случайная строка (32+ символов)
   - Environment: Production, Preview, Development
   - Если нет - сгенерируйте: `openssl rand -base64 32` или используйте https://generate-secret.vercel.app/32

2. **GOOGLE_CLIENT_ID** (обязательно!)
   - Значение: Client ID из Google Cloud Console (формат: `xxxxx.apps.googleusercontent.com`)
   - Environment: Production, Preview, Development

3. **GOOGLE_CLIENT_SECRET** (обязательно!)
   - Значение: Client Secret из Google Cloud Console
   - Environment: Production, Preview, Development

4. **DATABASE_URL** (обязательно!)
   - Значение: строка подключения из Neon Dashboard
   - Environment: Production, Preview, Development

### Шаг 2: Пересоздайте переменные окружения (если нужно)

Если переменные уже есть, но ошибка сохраняется:

1. Удалите переменные (кроме DATABASE_URL, если она работает)
2. Добавьте их заново
3. Убедитесь, что выбраны **все окружения** (Production, Preview, Development)
4. Сохраните изменения

### Шаг 3: Передеплойте проект

После добавления/изменения переменных окружения:

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) → **"Redeploy"**
4. Выберите **"Use existing Build Cache"** = OFF (чтобы пересобрать с новыми переменными)
5. Нажмите **"Redeploy"**

**ИЛИ** через Git:

```powershell
# Сделайте любой коммит (например, добавьте комментарий в README)
git commit --allow-empty -m "Trigger redeploy"
git push
```

### Шаг 4: Проверьте логи Vercel

1. В Vercel Dashboard → ваш проект → **Deployments**
2. Откройте последний деплой
3. Перейдите в **"Functions"** → найдите `/api/auth/[...nextauth]`
4. Посмотрите логи - там должна быть более подробная ошибка

## 🔧 Частые причины ошибки

### 1. AUTH_SECRET не установлен

**Ошибка в логах:**
```
AUTH_SECRET is missing
```

**Решение:**
- Добавьте `AUTH_SECRET` в Environment Variables на Vercel
- Используйте случайную строку (32+ символов)

### 2. GOOGLE_CLIENT_ID или GOOGLE_CLIENT_SECRET не установлены

**Ошибка в логах:**
```
Missing GOOGLE_CLIENT_ID or GOOGLE_CLIENT_SECRET
```

**Решение:**
- Добавьте оба в Environment Variables на Vercel
- Проверьте, что значения скопированы правильно (без лишних пробелов)

### 3. Неправильный Redirect URI в Google Cloud Console

**Ошибка:**
- Редирект происходит, но авторизация не завершается

**Решение:**
1. Откройте [Google Cloud Console](https://console.cloud.google.com/apis/credentials)
2. Найдите ваш OAuth 2.0 Client ID
3. Проверьте, что в **Authorized redirect URIs** есть:
   ```
   https://pro-store-nine.vercel.app/api/auth/callback/google
   ```
4. Сохраните изменения

### 4. Переменные установлены только для Production

**Проблема:**
- Переменные установлены только для Production, но не для Preview/Development

**Решение:**
- При добавлении переменных выберите **все три** окружения:
  - ✅ Production
  - ✅ Preview
  - ✅ Development

### 5. Переменные установлены, но проект не пересобран

**Проблема:**
- Переменные добавлены, но проект использует старую сборку

**Решение:**
- Сделайте **Redeploy** (см. Шаг 3)

## 📋 Чеклист проверки

- [ ] `AUTH_SECRET` установлен на Vercel (32+ символов)
- [ ] `GOOGLE_CLIENT_ID` установлен на Vercel
- [ ] `GOOGLE_CLIENT_SECRET` установлен на Vercel
- [ ] `DATABASE_URL` установлен на Vercel
- [ ] Все переменные установлены для Production, Preview и Development
- [ ] Redirect URI добавлен в Google Cloud Console: `https://pro-store-nine.vercel.app/api/auth/callback/google`
- [ ] Проект передеплоен после добавления переменных
- [ ] Проверены логи в Vercel Dashboard для подробной ошибки

## 🐛 Как проверить логи на Vercel

1. Откройте Vercel Dashboard → ваш проект
2. Перейдите в **Deployments** → последний деплой
3. Нажмите на деплой, чтобы открыть детали
4. Перейдите в **"Functions"** (вкладка)
5. Найдите `/api/auth/[...nextauth]`
6. Нажмите на него - откроются логи
7. Ищите ошибки (они будут выделены красным)

## 🎯 Быстрая проверка

### Генерация AUTH_SECRET

Если нужно сгенерировать `AUTH_SECRET`:

**Вариант 1: Онлайн генератор**
```
https://generate-secret.vercel.app/32
```

**Вариант 2: PowerShell (локально)**
```powershell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

**Вариант 3: OpenSSL (если установлен)**
```bash
openssl rand -base64 32
```

### Проверка значений переменных

⚠️ **НЕ копируйте значения переменных в публичные места!**

Но убедитесь, что:
- `GOOGLE_CLIENT_ID` заканчивается на `.apps.googleusercontent.com`
- `GOOGLE_CLIENT_SECRET` - длинная строка без пробелов
- `AUTH_SECRET` - случайная строка (32+ символов)
- `DATABASE_URL` начинается с `postgresql://`

## 💡 Советы

1. **Всегда проверяйте логи** - они дают точную причину ошибки
2. **Передеплойте после изменений** - переменные окружения применяются только при новой сборке
3. **Используйте одинаковые переменные для всех окружений** - это упрощает отладку
4. **Проверьте, что значения скопированы без лишних пробелов** - это частая ошибка

## 📞 Если ничего не помогло

1. Проверьте логи в Vercel Dashboard (см. раздел "Как проверить логи")
2. Убедитесь, что локально всё работает (`npm run dev`)
3. Сравните `.env` локально с переменными на Vercel
4. Убедитесь, что Google OAuth настроен правильно (см. `AUTH_SETUP.md`)





