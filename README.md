# ProStore - Next.js + Prisma + NeonDB

Минимальный рабочий проект на Next.js (App Router) с Prisma и NeonDB (PostgreSQL), готовый к деплою на Vercel.

## Технологии

- **Next.js 14** (App Router, TypeScript)
- **Prisma** (ORM)
- **NeonDB** (PostgreSQL)
- **Vercel** (деплой)

## Быстрый старт

### 1. Установка зависимостей

```powershell
npm install
```

### 2. Настройка базы данных

1. Создайте аккаунт на [Neon](https://neon.tech)
2. Создайте новый проект и базу данных
3. Скопируйте строку подключения (Connection String) из Dashboard
4. Создайте файл `.env` на основе `.env.example`:

```powershell
Copy-Item .env.example .env
```

5. Вставьте строку подключения в `.env`:

```
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
```

### 3. Настройка Prisma

Примените схему к базе данных:

```powershell
npx prisma db push
```

Или создайте миграцию:

```powershell
npx prisma migrate dev --name init
```

### 4. Заполнение базы данных (seed)

```powershell
npm run db:seed
```

### 5. Запуск проекта

```powershell
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Деплой на Vercel

### 1. Подготовка

1. Убедитесь, что все изменения закоммичены в Git:
```powershell
git add .
git commit -m "Подготовка к деплою на Vercel"
```

2. Создайте репозиторий на GitHub/GitLab/Bitbucket и запушьте код:
```powershell
git remote add origin <your-repo-url>
git push -u origin main
```

### 2. Деплой через Vercel Dashboard

1. Перейдите на [vercel.com](https://vercel.com) и войдите в аккаунт
2. Нажмите **"Add New Project"** или **"Import Project"**
3. Импортируйте ваш Git репозиторий
4. Vercel автоматически определит Next.js проект
5. **ВАЖНО**: В разделе **"Environment Variables"** добавьте переменную:
   - **Name**: `DATABASE_URL`
   - **Value**: ваша строка подключения из Neon Dashboard
   - **Environment**: выберите Production, Preview и Development (или только Production)
6. Нажмите **"Deploy"**

### 3. Деплой через Vercel CLI

```powershell
# Установка Vercel CLI (если еще не установлен)
npm i -g vercel

# Логин в Vercel
vercel login

# Первый деплой (следуйте инструкциям)
vercel

# Добавление переменной окружения
vercel env add DATABASE_URL production
# Введите значение при запросе

# Деплой в продакшн
vercel --prod
```

### 4. Настройка базы данных после деплоя

После успешного деплоя примените схему к продакшн-базе:

**Вариант 1: Через локальную машину**
```powershell
# Установите DATABASE_URL для продакшн (из Neon Dashboard)
$env:DATABASE_URL="your-production-database-url"

# Примените схему
npx prisma db push

# Заполните данными (опционально)
npm run db:seed
```

**Вариант 2: Через Vercel CLI**
```powershell
# Получите URL проекта
vercel env pull .env.production

# Примените схему
npx prisma db push

# Заполните данными
npm run db:seed
```

**Вариант 3: Через Prisma Studio**
```powershell
# Установите DATABASE_URL
$env:DATABASE_URL="your-production-database-url"

# Откройте Prisma Studio
npx prisma studio
```

### 5. Проверка деплоя

1. После деплоя откройте URL вашего проекта (например, `https://your-project.vercel.app`)
2. Если видите список заметок — деплой успешен! ✅
3. Если видите сообщение об отсутствии данных — выполните шаг 4 (настройка БД)

### 6. Автоматические деплои

После первого деплоя Vercel автоматически будет деплоить:
- **Production**: при пуше в `main`/`master` ветку
- **Preview**: при создании Pull Request или пуше в другие ветки

Все переменные окружения из Production автоматически применяются к Preview деплоям.

## Структура проекта

```
.
├── app/
│   ├── layout.tsx      # Корневой layout
│   ├── page.tsx        # Главная страница с чтением из БД
│   └── globals.css     # Глобальные стили
├── lib/
│   └── prisma.ts       # Singleton Prisma Client
├── prisma/
│   ├── schema.prisma   # Схема Prisma
│   └── seed.ts         # Seed скрипт
├── .env.example        # Пример переменных окружения
├── next.config.js      # Конфигурация Next.js
├── package.json        # Зависимости и скрипты
├── tsconfig.json       # Конфигурация TypeScript
└── vercel.json         # Конфигурация Vercel
```

## Модель данных

### Note

- `id` (String, UUID) - уникальный идентификатор
- `title` (String) - заголовок заметки
- `createdAt` (DateTime) - дата создания

## Полезные команды

```powershell
# Разработка
npm run dev

# Сборка
npm run build

# Запуск продакшн-версии
npm start

# Prisma команды
npx prisma db push              # Применить схему без миграции
npx prisma migrate dev          # Создать и применить миграцию
npx prisma generate             # Сгенерировать Prisma Client
npx prisma studio               # Открыть Prisma Studio

# Seed
npm run db:seed
```

## Примечания

- ✅ **Готово к деплою**: Проект полностью настроен для деплоя на Vercel
- 🔐 **Переменные окружения**: При деплое на Vercel переменная `DATABASE_URL` должна быть установлена в настройках проекта
- 🔧 **Автоматическая генерация**: Скрипт `postinstall` автоматически генерирует Prisma Client при деплое
- 🚀 **Connection Pooling**: Для продакшн-базы рекомендуется использовать connection pooling (Neon предоставляет это автоматически)
- 📦 **Build оптимизация**: Скрипт `build` включает генерацию Prisma Client перед сборкой Next.js
- 🌍 **Регион**: По умолчанию проект деплоится в регион `iad1` (можно изменить в `vercel.json`)

