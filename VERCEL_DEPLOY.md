# Инструкция по деплою ProStore на Vercel

## 🔍 Проблема

После деплоя на Vercel отображается только главная страница со списком заметок, а не личный кабинет.

## ✅ Решение

### 1. Обновлена главная страница

Главная страница (`app/page.tsx`) теперь редиректит:
- На `/dashboard` - если пользователь авторизован
- На `/login` - если не авторизован

### 2. Настройка переменных окружения на Vercel

Для работы личного кабинета необходимо настроить переменные окружения:

#### Шаг 1: Откройте Vercel Dashboard

1. Перейдите на [https://vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект ProStore
3. Перейдите в **Settings** → **Environment Variables**

#### Шаг 2: Добавьте переменные

Добавьте следующие переменные окружения:

1. **DATABASE_URL** (обязательно)
   - **Value**: строка подключения из Neon Dashboard
   - **Environment**: Production, Preview, Development

2. **AUTH_SECRET** (обязательно)
   - **Value**: случайная строка (32+ символов)
   - Можно сгенерировать: `openssl rand -base64 32`
   - **Environment**: Production, Preview, Development

3. **GOOGLE_CLIENT_ID** (обязательно)
   - **Value**: Client ID из Google Cloud Console
   - **Environment**: Production, Preview, Development

4. **GOOGLE_CLIENT_SECRET** (обязательно)
   - **Value**: Client Secret из Google Cloud Console
   - **Environment**: Production, Preview, Development

#### Шаг 3: Обновите Google OAuth Redirect URIs

В Google Cloud Console добавьте Redirect URI для продакшна:

1. **Найдите домен вашего проекта на Vercel:**
   - Откройте [Vercel Dashboard](https://vercel.com/dashboard)
   - Выберите ваш проект ProStore
   - В верхней части страницы увидите домен (например: `prostore.vercel.app`)
   - Или посмотрите в разделе **Deployments** → последний деплой → ссылка **Visit**

2. **Добавьте Redirect URI в Google Cloud Console:**
   - Откройте [Google Cloud Console](https://console.cloud.google.com/)
   - Выберите ваш проект
   - Перейдите в **APIs & Services** → **Credentials**
   - Найдите ваш OAuth 2.0 Client ID
   - Нажмите на него для редактирования
   - В разделе **Authorized redirect URIs** нажмите **+ ADD URI**
   - Добавьте:
     ```
     https://ваш-домен.vercel.app/api/auth/callback/google
     ```
     Например: `https://prostore.vercel.app/api/auth/callback/google`
   - Нажмите **SAVE**

### 3. Применение Prisma схемы к продакшн-базе

#### Вариант 1: Через локальную команду (рекомендуется)

```powershell
# Установите переменную окружения с продакшн-строкой подключения
$env:DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Примените схему к базе данных
npx prisma db push

# (Опционально) Заполните базу тестовыми данными
npm run db:seed
```

#### Вариант 2: Через Vercel CLI

```powershell
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Логин
vercel login

# Получите переменные окружения
vercel env pull .env.production

# Примените схему
npx prisma db push

# (Опционально) Заполните данными
npm run db:seed
```

### 4. Передеплой проекта

После настройки переменных окружения:

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **Redeploy** → **Redeploy**

Или через Git:

```powershell
# Сделайте коммит изменений
git add .
git commit -m "Обновление главной страницы - редирект на dashboard/login"
git push
```

Vercel автоматически пересоберет проект.

## 🔍 Проверка работы

После деплоя:

1. Откройте ваш домен на Vercel
2. Должен произойти редирект на `/login`
3. Нажмите "Войти через Google"
4. После входа - редирект на `/dashboard`
5. Личный кабинет должен работать

## 🐛 Решение проблем

### Ошибка "Server error" на странице входа

**Причина**: Не настроены переменные окружения для Auth.js

**Решение**:
- Проверьте, что `AUTH_SECRET` установлен
- Проверьте, что `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` правильные
- Проверьте, что Redirect URI добавлен в Google Cloud Console

### Ошибка "Database connection failed"

**Причина**: Неправильный `DATABASE_URL` или база недоступна

**Решение**:
- Проверьте строку подключения из Neon Dashboard
- Убедитесь, что `DATABASE_URL` установлен для Production окружения
- Проверьте, что в строке подключения есть `?sslmode=require`

### Главная страница не редиректит

**Причина**: Кэш или старая версия

**Решение**:
- Сделайте Hard Refresh (Ctrl+Shift+R или Cmd+Shift+R)
- Очистите кэш браузера
- Проверьте, что последний деплой успешен

### Prisma схема не применена

**Причина**: `prisma db push` не выполнен для продакшн-базы

**Решение**:
```powershell
# Установите DATABASE_URL для продакшна
$env:DATABASE_URL="ваша-продакшн-строка-подключения"

# Примените схему
npx prisma db push
```

## 📋 Чеклист перед деплоем

- [ ] Переменные окружения настроены на Vercel:
  - [ ] `DATABASE_URL`
  - [ ] `AUTH_SECRET`
  - [ ] `GOOGLE_CLIENT_ID`
  - [ ] `GOOGLE_CLIENT_SECRET`
- [ ] Redirect URI добавлен в Google Cloud Console для продакшна
- [ ] Prisma схема применена к продакшн-базе (`npx prisma db push`)
- [ ] Главная страница обновлена (редирект на `/dashboard`/`/login`)
- [ ] Проект успешно собирается локально (`npm run build`)

## 📝 Важные замечания

1. **Переменные окружения** должны быть установлены для всех окружений (Production, Preview, Development), иначе они не будут доступны во время сборки

2. **AUTH_SECRET** должен быть одинаковым для всех окружений, иначе сессии не будут работать

3. **Google OAuth Redirect URIs** должны включать:
   - `http://localhost:3000/api/auth/callback/google` (для локальной разработки)
   - `https://ваш-домен.vercel.app/api/auth/callback/google` (для продакшна)

4. **Prisma схема** должна быть применена к продакшн-базе до первого использования

5. **DATABASE_URL** должен быть доступен из интернета (Neon по умолчанию доступна)

