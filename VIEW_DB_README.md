# View DB - Просмотр и управление базой данных

Тестовая программа для просмотра и управления данными в базе данных через веб-интерфейс.

## Возможности

- ✅ Выбор базы данных (локальная или рабочая)
- ✅ Просмотр списка всех таблиц
- ✅ Просмотр данных таблицы с пагинацией
- ✅ CRUD операции (Create, Read, Update, Delete)
- ✅ Редактирование записей прямо в таблице
- ✅ Создание новых записей через модальное окно

## Использование

### 1. Настройка переменных окружения

Добавьте в файл `.env`:

```env
# Локальная база данных
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Рабочая база данных (опционально)
DATABASE_URL_PRODUCTION="postgresql://user:password@host/production-database?sslmode=require"
```

Если `DATABASE_URL_PRODUCTION` не указан, будет использоваться `DATABASE_URL` для рабочей БД.

### 2. Запуск приложения

```powershell
npm run dev
```

### 3. Открыть View DB

Перейдите по адресу: [http://localhost:3000/view-db](http://localhost:3000/view-db)

## Структура приложения

```
/view-db                    # Выбор базы данных
  ↓
/view-db/tables?db=local     # Список таблиц
  ↓
/view-db/table/[name]?db=local  # Просмотр таблицы с CRUD
```

## Доступные таблицы

- **user** - Пользователи
- **note** - Заметки
- **category** - Категории
- **film** - Фильмы
- **tag** - Теги
- **filmTag** - Связь фильмов и тегов (многие-ко-многим)
- **vote** - Голоса

## Функции

### Просмотр таблицы

- Автоматическая пагинация (10 записей на страницу)
- Навигация между страницами
- Отображение всех колонок таблицы

### Создание записи (Create)

1. Нажмите кнопку **"+ Создать"**
2. Заполните поля в модальном окне
3. Нажмите **"Создать"**

### Редактирование записи (Update)

1. Нажмите кнопку **✏️** рядом с записью
2. Измените значения в полях
3. Нажмите **✓** для сохранения или **✕** для отмены

### Удаление записи (Delete)

1. Нажмите кнопку **🗑️** рядом с записью
2. Подтвердите удаление

## API Endpoints

### GET `/api/view-db/tables?db=local`
Получить список всех таблиц с количеством записей.

### GET `/api/view-db/table/[name]?db=local&page=1&limit=10`
Получить данные таблицы с пагинацией.

### POST `/api/view-db/table/[name]?db=local`
Создать новую запись.

### PUT `/api/view-db/table/[name]?db=local`
Обновить запись.

### DELETE `/api/view-db/table/[name]?db=local&id=...`
Удалить запись.

## Особенности

- **Безопасность**: Все операции выполняются через API с валидацией
- **Обработка ошибок**: Понятные сообщения об ошибках
- **Производительность**: Пагинация для больших таблиц
- **Удобство**: Интуитивный интерфейс

## Примечания

- Для таблицы `filmTag` используется составной ключ (filmId + tagId)
- Автоматические поля (createdAt, updatedAt) не редактируются
- При удалении записей с внешними ключами применяется каскадное удаление (если настроено в схеме)


