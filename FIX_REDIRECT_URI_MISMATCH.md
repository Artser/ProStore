# Исправление ошибки "redirect_uri_mismatch" в Google OAuth

## 🔍 Проблема

При попытке входа через Google появляется ошибка:
```
Ошибка 400: redirect_uri_mismatch
```

Это означает, что redirect URI, который отправляет ваше приложение, **не совпадает** с зарегистрированными в Google Cloud Console.

## ✅ Решение

### Шаг 1: Определите ваш домен Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект **pro-store-nine** (или ваш проект)
3. В верхней части страницы найдите домен проекта
   - Например: `pro-store-nine.vercel.app` или `your-custom-domain.com`
4. **Скопируйте домен** (он понадобится в следующем шаге)

### Шаг 2: Откройте Google Cloud Console

1. Перейдите на [Google Cloud Console - Credentials](https://console.cloud.google.com/apis/credentials)
2. Выберите ваш проект (или создайте новый)
3. Найдите ваш **OAuth 2.0 Client ID** (или создайте новый)
4. Нажмите на **редактирование** (иконка карандаша) вашего Client ID

### Шаг 3: Добавьте Authorized redirect URIs

В разделе **"Authorized redirect URIs"** добавьте **ВСЕ** следующие URI:

#### Для продакшна (Vercel):
```
https://pro-store-nine.vercel.app/api/auth/callback/google
```

**Замените `pro-store-nine.vercel.app` на ваш реальный домен!**

#### Для локальной разработки:
```
http://localhost:3000/api/auth/callback/google
```

#### Если у вас есть кастомный домен:
```
https://your-custom-domain.com/api/auth/callback/google
```

#### Для Preview деплоев (опционально):
Если вы хотите, чтобы OAuth работал и на preview деплоях, добавьте:
```
https://*.vercel.app/api/auth/callback/google
```

**⚠️ ВАЖНО:**
- URI должны быть **точными** (включая `https://` или `http://`)
- **Без слеша в конце** (`/api/auth/callback/google` ✅, не `/api/auth/callback/google/` ❌)
- Регистр не важен, но лучше использовать строчные буквы
- Для локальной разработки используйте `http://` (не `https://`)

### Шаг 4: Сохраните изменения

1. Нажмите **"Save"** (Сохранить) внизу страницы
2. Дождитесь подтверждения сохранения

### Шаг 5: Проверьте переменные окружения на Vercel

Убедитесь, что на Vercel установлены правильные переменные:

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект → **Settings** → **Environment Variables**
3. Проверьте:
   - ✅ `GOOGLE_CLIENT_ID` - должен совпадать с Client ID из Google Cloud Console
   - ✅ `GOOGLE_CLIENT_SECRET` - должен совпадать с Client Secret из Google Cloud Console
   - ✅ `AUTH_SECRET` - должен быть установлен
   - ✅ `DATABASE_URL` - должен быть установлен

### Шаг 6: Передеплойте проект (если нужно)

Если вы изменили переменные окружения:

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** → **"Redeploy"**
4. Выберите **"Use existing Build Cache"** = OFF
5. Нажмите **"Redeploy"**

**ИЛИ** сделайте пустой коммит:

```powershell
git commit --allow-empty -m "Trigger redeploy after OAuth fix"
git push
```

### Шаг 7: Проверьте результат

1. Откройте ваш сайт на Vercel: `https://pro-store-nine.vercel.app`
2. Перейдите на страницу входа: `https://pro-store-nine.vercel.app/login`
3. Нажмите "Войти через Google"
4. Должен произойти редирект на Google для авторизации
5. После авторизации вы должны вернуться на сайт

## 🔧 Частые ошибки

### Ошибка 1: "URI не добавлен в Google Cloud Console"

**Симптомы:**
- Ошибка `redirect_uri_mismatch` продолжается
- В логах видно, что используется другой URI

**Решение:**
1. Проверьте, что URI добавлен **точно** как в инструкции выше
2. Убедитесь, что вы сохранили изменения в Google Cloud Console
3. Подождите 1-2 минуты (изменения могут применяться с задержкой)

### Ошибка 2: "Используется неправильный домен"

**Симптомы:**
- Ошибка появляется только на Vercel, локально работает
- Или наоборот

**Решение:**
1. Проверьте, какой домен используется на Vercel
2. Убедитесь, что в Google Cloud Console добавлен **правильный** домен
3. Для локальной разработки используйте `http://localhost:3000`
4. Для Vercel используйте `https://your-domain.vercel.app`

### Ошибка 3: "Лишний слеш в конце URI"

**Симптомы:**
- URI добавлен, но ошибка продолжается

**Решение:**
- Убедитесь, что URI **без слеша в конце**:
  - ✅ `https://domain.com/api/auth/callback/google`
  - ❌ `https://domain.com/api/auth/callback/google/`

### Ошибка 4: "Используется http вместо https"

**Симптомы:**
- Ошибка на Vercel (где должен быть https)

**Решение:**
- Для Vercel всегда используйте `https://`
- Для localhost используйте `http://`

## 📋 Чеклист проверки

- [ ] Домен Vercel определен (например: `pro-store-nine.vercel.app`)
- [ ] В Google Cloud Console добавлен URI: `https://your-domain.vercel.app/api/auth/callback/google`
- [ ] Для локальной разработки добавлен URI: `http://localhost:3000/api/auth/callback/google`
- [ ] Изменения сохранены в Google Cloud Console
- [ ] Переменные `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` установлены на Vercel
- [ ] Проект передеплоен (если изменялись переменные окружения)
- [ ] Проверен вход через Google на продакшне

## 🎯 Быстрая проверка URI

### Формат URI для Auth.js (NextAuth v5):

```
{protocol}://{domain}/api/auth/callback/{provider}
```

Где:
- `{protocol}` = `https` для Vercel, `http` для localhost
- `{domain}` = ваш домен (например: `pro-store-nine.vercel.app`)
- `{provider}` = `google` (для Google OAuth)

**Примеры правильных URI:**
- ✅ `https://pro-store-nine.vercel.app/api/auth/callback/google`
- ✅ `http://localhost:3000/api/auth/callback/google`
- ✅ `https://my-custom-domain.com/api/auth/callback/google`

**Примеры неправильных URI:**
- ❌ `https://pro-store-nine.vercel.app/api/auth/callback/google/` (лишний слеш)
- ❌ `http://pro-store-nine.vercel.app/api/auth/callback/google` (http вместо https)
- ❌ `https://pro-store-nine.vercel.app/auth/callback/google` (неправильный путь)

## 💡 Дополнительная информация

### Как найти точный URI, который использует приложение:

1. Откройте DevTools в браузере (F12)
2. Перейдите на вкладку **Network** (Сеть)
3. Попробуйте войти через Google
4. Найдите запрос к `accounts.google.com`
5. В параметрах запроса найдите `redirect_uri`
6. Скопируйте этот URI и добавьте его в Google Cloud Console

### Если ничего не помогает:

1. Проверьте логи Vercel:
   - Vercel Dashboard → проект → **Deployments** → последний деплой → **Functions** → `/api/auth/[...nextauth]`
   - Ищите ошибки в логах

2. Проверьте, что используется правильный Client ID:
   - В Google Cloud Console найдите ваш Client ID
   - Убедитесь, что на Vercel установлена **та же** переменная `GOOGLE_CLIENT_ID`

3. Попробуйте создать новый OAuth Client ID:
   - В Google Cloud Console создайте новый Client ID
   - Обновите переменные на Vercel
   - Передеплойте проект

## 📞 Полезные ссылки

- [Google Cloud Console - Credentials](https://console.cloud.google.com/apis/credentials)
- [Vercel Dashboard](https://vercel.com/dashboard)
- [Auth.js Documentation](https://authjs.dev)



