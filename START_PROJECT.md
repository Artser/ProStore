# Инструкция по запуску проекта ProStore с авторизацией

## 🚀 Быстрый старт

### 1. Установка зависимостей

```powershell
npm install
```

**Примечание:** Если появится ошибка EPERM при установке - это не критично, зависимости уже установлены.

### 2. Настройка переменных окружения

Убедитесь, что файл `.env` содержит все необходимые переменные:

```env
# База данных
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Google OAuth (получите из Google Cloud Console)
GOOGLE_CLIENT_ID="ваш-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="ваш-client-secret"

# Auth.js Secret
AUTH_SECRET="ваш-случайный-секрет-32-символа"
```

### 3. Применение схемы базы данных

```powershell
npx prisma db push
```

Это создаст/обновит таблицы в базе данных.

### 4. Генерация Prisma Client

```powershell
npx prisma generate
```

**Примечание:** Если появится ошибка EPERM - это не критично, Prisma Client уже сгенерирован.

### 5. Запуск dev-сервера

```powershell
npm run dev
```

Сервер запустится на [http://localhost:3000](http://localhost:3000)

### 6. Откройте страницу входа

Перейдите на: [http://localhost:3000/login](http://localhost:3000/login)

## 📋 Пошаговая инструкция

### Шаг 1: Настройка Google OAuth

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 Client ID:
   - Тип: Web application
   - Authorized redirect URIs: `http://localhost:3000/api/auth/callback/google`
5. Скопируйте Client ID и Client Secret

### Шаг 2: Настройка .env

Создайте файл `.env` в корне проекта (если его нет):

```env
DATABASE_URL="postgresql://..."
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."
AUTH_SECRET="..."  # Сгенерируйте случайную строку
```

### Шаг 3: Запуск проекта

```powershell
# Остановите все процессы Node.js (если запущены)
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force

# Запустите dev-сервер
npm run dev
```

### Шаг 4: Вход в систему

1. Откройте [http://localhost:3000/login](http://localhost:3000/login)
2. Нажмите "Войти через Google"
3. Разрешите доступ в Google
4. После успешного входа вы будете перенаправлены на `/dashboard`

## 🎯 Использование личного кабинета

После входа через Google:

### Доступные страницы:

- **Мои фильмы**: [http://localhost:3000/dashboard](http://localhost:3000/dashboard)
  - Просмотр всех ваших фильмов
  - Создание новых фильмов
  - Редактирование и удаление

- **Публичные фильмы**: [http://localhost:3000/dashboard/public](http://localhost:3000/dashboard/public)
  - Все опубликованные фильмы от всех пользователей

- **Избранное**: [http://localhost:3000/dashboard/favorites](http://localhost:3000/dashboard/favorites)
  - Ваши избранные фильмы

- **История**: [http://localhost:3000/dashboard/history](http://localhost:3000/dashboard/history)
  - (Заглушка - функциональность будет добавлена позже)

- **Настройки**: [http://localhost:3000/dashboard/settings](http://localhost:3000/dashboard/settings)
  - (Заглушка - функциональность будет добавлена позже)

### Основные функции:

1. **Создание фильма:**
   - Нажмите "+ Новый фильм"
   - Заполните форму
   - Выберите публичность
   - Нажмите "Создать"

2. **Редактирование:**
   - Нажмите на иконку "⋮" на карточке
   - Выберите "Редактировать"
   - Внесите изменения
   - Сохраните

3. **Удаление:**
   - Нажмите на иконку "⋮"
   - Выберите "Удалить"
   - Подтвердите

4. **Избранное:**
   - Нажмите на иконку звезды
   - Фильм добавится в избранное

5. **Публичность:**
   - Нажмите на иконку "⋮"
   - Выберите "Опубликовать" / "Сделать приватным"

## 🔧 Решение проблем

### Ошибка "missing required error components"
✅ Уже исправлено - создан файл `app/error.tsx`

### Ошибка EPERM при npm install
- Не критично - зависимости уже установлены
- Или остановите процессы Node.js: `Get-Process node | Stop-Process -Force`

### Ошибка при входе через Google
- Проверьте переменные `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` в `.env`
- Убедитесь, что redirect URI добавлен в Google Cloud Console
- Проверьте, что `AUTH_SECRET` установлен

### Страница не загружается
1. Проверьте логи в терминале (где запущен `npm run dev`)
2. Проверьте консоль браузера (F12)
3. Убедитесь, что dev-сервер запущен

## 📝 Полезные команды

```powershell
# Разработка
npm run dev

# Сборка
npm run build

# Запуск продакшн-версии
npm start

# Prisma
npx prisma db push        # Применить схему
npx prisma generate       # Сгенерировать Client
npx prisma studio         # Открыть GUI для БД

# Seed (заполнение БД тестовыми данными)
npm run db:seed
```

## ✅ Проверка готовности

Проект готов к работе, если:
- ✅ Зависимости установлены
- ✅ Файл `.env` настроен
- ✅ Prisma схема применена (`npx prisma db push`)
- ✅ Dev-сервер запущен (`npm run dev`)
- ✅ Страница `/login` открывается

После этого вы можете войти через Google и использовать личный кабинет!

