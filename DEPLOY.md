# Чек-лист деплоя на Vercel

## ✅ Перед деплоем

- [ ] Все изменения закоммичены в Git
- [ ] Код запушен в репозиторий (GitHub/GitLab/Bitbucket)
- [ ] Создан аккаунт на [Neon](https://neon.tech)
- [ ] Создана база данных в Neon
- [ ] Скопирована строка подключения (Connection String) из Neon Dashboard
- [ ] Локально протестирован проект (`npm run dev`)
- [ ] Локально применена схема БД (`npx prisma db push`)
- [ ] Локально заполнена БД тестовыми данными (`npm run db:seed`)

## 🚀 Деплой на Vercel

### Через Dashboard (рекомендуется)

- [ ] Войти на [vercel.com](https://vercel.com)
- [ ] Нажать "Add New Project"
- [ ] Импортировать Git репозиторий
- [ ] **Добавить переменную окружения `DATABASE_URL`** (важно!)
- [ ] Нажать "Deploy"
- [ ] Дождаться завершения сборки

### Через CLI

- [ ] Установлен Vercel CLI (`npm i -g vercel`)
- [ ] Выполнен логин (`vercel login`)
- [ ] Выполнен деплой (`vercel`)
- [ ] Добавлена переменная `DATABASE_URL` (`vercel env add DATABASE_URL`)
- [ ] Выполнен продакшн деплой (`vercel --prod`)

## 🔧 После деплоя

- [ ] Применить схему к продакшн-базе:
  ```powershell
  $env:DATABASE_URL="your-production-database-url"
  npx prisma db push
  ```
- [ ] Заполнить продакшн-базу данными (опционально):
  ```powershell
  npm run db:seed
  ```
- [ ] Открыть URL проекта и проверить работу
- [ ] Убедиться, что данные отображаются корректно

## 🐛 Решение проблем

### Ошибка "Prisma Client не найден"
- Убедитесь, что переменная `DATABASE_URL` установлена в Vercel
- Проверьте логи сборки в Vercel Dashboard

### Ошибка подключения к БД
- Проверьте строку подключения в переменных окружения Vercel
- Убедитесь, что БД доступна из интернета (Neon по умолчанию доступна)
- Проверьте, что используется правильная строка подключения (production)

### Страница пустая или показывает ошибку
- Проверьте логи в Vercel Dashboard (Functions → Logs)
- Убедитесь, что схема применена к БД (`npx prisma db push`)
- Проверьте, что в БД есть данные

## 📝 Полезные команды

```powershell
# Проверка локальной сборки
npm run build

# Просмотр логов Vercel
vercel logs

# Просмотр переменных окружения
vercel env ls

# Откат к предыдущей версии
vercel rollback
```


